// Step 1: Ask the user a question
let answer = prompt("What is the capital of France?");

// Step 2: Check if the user's answer is correct
if (answer.toLowerCase() === "paris") {
    // Step 3: Display a message if the answer is correct
    alert("Correct! The capital of France is Paris.");
} else {
    // Step 3: Display a message if the answer is incorrect
    alert("Incorrect. The capital of France is Paris.");
}
